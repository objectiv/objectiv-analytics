# TODO: add hashes
# TODO: split between production and development (e.g. have mypy there)
# TODO: make backends (postgres?) optional
boto3
jsonschema
flask
flask-cors
psycopg2-binary
# in the future we should maybe split this into different kind of workers. And not have dbt installed on the http endpoint
dbt
